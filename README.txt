dolmen.viewlet
**************

``dolmen.viewlet`` provides a base component for the `IViewSlot` interface
defined by ``cromlech.browser`` and defines a sub components, called
``Viewlet`` and providing the ``IViewlet`` interface.
